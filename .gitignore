node_modules/
dist/
coverage/
*.log
*.tsbuildinfo

# real env files — only .env.example should be committed
.env
.env.*
!.env.example

# Local dev's event media uploads (UPLOAD_DIR defaults here when unset) —
# runtime data, not source; production uses a mounted volume instead.
apps/api/uploads/

.DS_Store

# Python bytecode (scripts/server/admin-agent.py)
__pycache__/
*.pyc
