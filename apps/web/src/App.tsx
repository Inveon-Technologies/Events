// Placeholder root component. Public event site + organizer portal routing
// (react-router-dom) lands with the Phase 1 FE tasks — this just proves the
// build/lint/test/deploy pipeline works end to end before any real UI exists.
export default function App() {
  return (
    <main>
      <h1>Inveon Events</h1>
      <p>Plan. Book. Celebrate.</p>
    </main>
  );
}
